import React, { useContext } from 'react'
import { Abouts } from './Aboutpage'

const Contactoage = () => {
    const users=useContext(Abouts)
    console.log("users",users)
  return (
    <div>

    </div>
  )
}

export default Contactoage